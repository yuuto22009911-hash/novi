---
'@novi-ui/core': patch
---

契約の JSDoc に `@a11y` と `@keywords` を追加した。

型定義に含まれるためエディタの補完でも読める。実行時の API は変わらない。

これらは AI 向け出力（`llms.txt` / `@novi-ui/mcp`）の生成元になる。
アクセシビリティ注記を契約に置くことで、docs・llms・MCP の3箇所が
同じ記述を読むようになり、書き分けによるズレが起きなくなる。
